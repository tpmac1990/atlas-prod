


describe('inspect each filter group in the primary and related filters for the title dataset', () => {

    before(() => {
        cy.visit('http://localhost:8000')
    })

    it('open filter', () => {

    })

    it('open location group', () => {

    })
})